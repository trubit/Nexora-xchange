FROM node:22-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci --omit=dev

COPY server ./server

# Port must match PORT env var (default 5001 in .env.example)
EXPOSE 5001

CMD ["node", "server/index.js"]